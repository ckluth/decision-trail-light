# Architecture

- Last updated: <YYYY-MM-DD>

This document is the authoritative, always-current description of
<Project Name>'s architecture. It is filled in incrementally as the project
grows — read it before any significant architectural change, and update it
(plus the `Last updated` line above) in the same change if it affects a
statement documented here.

_Populate the sections below with the project's actual current architecture.
If adopting this template into a brand-new, still-empty repo, say so
explicitly here (e.g. "Nothing has been built yet") instead of leaving stale
placeholder text once real content is due._

## Overview

<What the system is, at a glance.>

## Components

<The major pieces and how they cooperate.>

## Key conventions

<Patterns/constraints that span multiple components.>
